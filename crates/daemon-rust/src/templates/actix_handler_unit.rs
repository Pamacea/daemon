#[cfg(test)]
mod tests {
    use super::*;

    #[actix_web::test]
    async fn test_index_handler() {
        // TODO: Implement test
        // let req = TestRequest::get().to_uri("/");
        // let resp = index(req).await;
        // assert_eq!(resp.status(), StatusCode::OK);
    }
}
