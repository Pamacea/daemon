#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_handler_returns_200() {
        // TODO: Implement actual test
        // let response = app.into_service().call(request).await;
        // assert_eq!(response.status(), StatusCode::OK);
    }
}
